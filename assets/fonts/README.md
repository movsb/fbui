# 字体

`full.ttf`是从机器上拷贝下来的。

## NerdFont字体生成

```bash
$ pyftsubset SymbolsNerdFontMono-Regular.ttf --unicodes='f07b' --output-file=nerd.ttf
```

## 等宽字体

```bash
$ pyftsubset MapleMonoNormalNL-Regular.ttf --text-file=mono.txt --output-file=mono.ttf
```
